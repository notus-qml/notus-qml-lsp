{
  "name": "notus-qml",
  "description": "Language server protocol for QML",
  "author": "None",
  "license": "MIT",
  "version": "1.0.0",
  "publisher": "None",
  "categories": [],
  "engines": {
    "vscode": "^1.75.0",
    "node": "22.11.0"
  },
  "activationEvents": [
    "onLanguage"
  ],
  "main": "./client/out/extension",
  "contributes": {},
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -b",
    "watch": "tsc -b -w",
    "postinstall": "cd client && npm install && cd ../server && npm install && cd .."
  },
  "devDependencies": {
    "@types/node": "^20.5.7",
    "tree-sitter-cli": "^0.25.3",
    "tsconfig-paths": "^4.2.0",
    "typescript": "^5.2.2"
  },
  "dependencies": {
    "@types/module-alias": "^2.0.4",
    "module-alias": "^2.2.3",
    "tree-sitter": "^0.22.4"
  }
}